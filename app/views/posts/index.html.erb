<div class="col-lg-12">
  <h1>NUACM Newsroom</h1>

  <ol id="posts">
    <% @posts.each do |post| %>
    <div class="entry">
      <h2><%= link_to post.title, post_path(post) %></h2>
      <p><%= post.body %></p>
    </div>
    <% end %>
  </ol>
</div>