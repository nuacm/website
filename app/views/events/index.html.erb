<h1>Events</h1>

<ol id="events">
  <% @events.each do |event| %>
  <div class="entry">
    <h2><%= link_to event.title, event_path(event) %></h2>
    <p><%= event.description %></p>
  </div>
  <% end %>
</ol>
